machine:
  python:
    version: 2.7.10
  services:
    - docker

dependencies:
  override:
    - docker build --no-cache -t hpeparagasg/restify-test .
    - npm install
test:
  override:
    # run test scripts
    - npm test
